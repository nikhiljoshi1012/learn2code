/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/learn2code',
    assetPrefix: '/learn2code',
    output: 'export',
    distDir: 'out',
    reactStrictMode: true,
};

export default nextConfig;
